{
  "name": "taskflow-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev:backend": "npm --prefix backend run start:dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "test": "npm --prefix backend run test",
    "test:e2e": "npm --prefix backend run test:e2e",
    "test:all": "npm --prefix backend run test && npm --prefix backend run test:e2e",
    "check:all": "npm --prefix backend run test && npm --prefix backend run test:e2e && npm run build"
  }
}
